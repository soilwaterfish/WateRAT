title: WateRAT
description: Water-right allocation analysis with reproducible state adapters and NHDPlus routing.
markdown: kramdown
permalink: pretty
defaults:
  - scope:
      path: ""
    values:
      layout: default
