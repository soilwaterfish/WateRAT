% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/water_right_schema.R
\name{select_mt_downstream_pods}
\alias{select_mt_downstream_pods}
\title{Select the downstream-most Montana POD for each water-right site}
\usage{
select_mt_downstream_pods(pods, comid_index, network)
}
\arguments{
\item{pods}{Montana \code{WRQS_PODS} records after eligibility filtering.}

\item{comid_index}{Output from \code{\link[=index_water_right_comids]{index_water_right_comids()}} run on
\code{\link[=standardize_mt_pod_candidates]{standardize_mt_pod_candidates()}}.}

\item{network}{NHDPlus flowlines containing \code{comid} and \code{hydroseq}.}
}
\value{
One raw Montana POD per \code{WRKEY}, with \code{selected_comid},
\code{pod_candidate_count}, and \code{pod_selection_method} audit fields.
}
\description{
NLDI assigns each candidate POD a COMID. Within each Montana \code{WRKEY}, the
candidate associated with the lowest NHDPlus \code{hydroseq} is retained because
HydroSeq decreases in the downstream direction. Ties are resolved by the
stable numeric \code{PODV_ID_SEQ}. If no candidate in a group has a COMID that is
present in the supplied network, the lowest POD ID is retained and marked as
a fallback for review.
}
